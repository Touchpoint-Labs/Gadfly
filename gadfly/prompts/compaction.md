Condense the file below while preserving every load-bearing fact, rule, decision,
and constraint. A reader must be able to enforce or navigate from the condensed
version alone. The user message states a CHARACTER BUDGET — your output must be
under that many characters.

- Keep every normative statement (must, must not, never, always) verbatim or nearly so.
- Keep every named design decision and its rationale; when decisions have been
  promoted into the file, weave them naturally into its narrative rather than
  keeping them as separate blocks.
- Keep every file/module name and its described role.
- Compress by dropping redundancy, repeated phrasings, and expendable exposition —
  never by dropping a fact or a rule.
- Merge near-duplicate entries into one that covers the union.
- Preserve the file's existing structure (headers, sections); don't reorganize.
- Shorter is better, but correctness is absolute. When in doubt, keep.

Return only the condensed file content, with no preamble, no explanation.
